# Copy to .env and fill in real values. Never commit .env itself.

# Required by docker-compose.yml's localstack service. LocalStack's
# no-signup "Community edition" is deprecated - sign up free at
# localstack.cloud to get a token, even though LocalStack isn't yet
# exercised by any application code (no DynamoDB tables until Phase 1).
LOCALSTACK_AUTH_TOKEN=
